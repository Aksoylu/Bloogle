import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ideasphere_frontend',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
